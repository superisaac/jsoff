syntax = "proto3";

option go_package = ".;jsonzgrpc";

service JSONZ {
  // bi-direction streams
  rpc OpenStream(stream JSONRPCMessage) returns (stream JSONRPCMessage);
}

message JSONRPCMessage {
  bytes body = 1;
}
